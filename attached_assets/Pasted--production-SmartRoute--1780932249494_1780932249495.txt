Проведи финальный production-аудит SmartRoute перед первым платным клиентом.

Контекст:

Проект работает на Railway.
PostgreSQL подключён.
Авторизация внедрена.
WhatsApp-маршруты исправлены.
Маршрутный лист реализован.
Недавно исправлялся CP Solver fail для маршрутов 120+ точек.
Домен подключён.
Это последний аудит перед демонстрацией клиенту.

Часть 1. Карта в production

Обнаружена проблема:

В Replit Canvas карта маршрута отображается корректно.
В Railway production карта не отображается либо отображается некорректно.

Проведи полный аудит:

Leaflet
marker icons
tile layers
static assets
production build Vite
CSP
CORS
BASE_URL
browser console errors
network errors
mixed content
HTTPS issues

Найди root cause и исправь.

Подтверди результат проверкой именно в production-сборке.

⸻

Часть 2. Временные окна (Time Windows)

Ответь конкретно:

Откуда сейчас берутся time windows для магазина?
Есть ли они в Excel импорте?
Если пользователь ничего не задаёт, какие значения используются?
Используются ли временные окна реально в текущем MVP?
Есть ли возможность полностью отключить их?
Покажи пример реального магазина из БД и какие tw_from / tw_to использовались при последнем построении маршрута на 120 точек.
После последнего фикса продолжают ли временные окна реально влиять на оптимизацию маршрута?

⸻

Часть 3. Финальное тестирование MVP

Проверь полностью:

вход в систему;
выход из системы;
импорт Excel;
шаблон Excel;
10 точек;
50 точек;
120 точек;
1 машина;
3 машины;
9 машин;
карта маршрута;
WhatsApp ссылки;
Яндекс ссылки;
маршрутный лист;
PDF/печать;
история маршрутов;
аналитика;
мобильная версия;
десктопная версия.

⸻

Часть 4. Поиск проблем перед первым клиентом

Найди всё, что может вызвать негативную реакцию клиента:

пустые экраны;
ошибки в консоли;
битые ссылки;
английский текст;
проблемы UX;
проблемы производительности;
проблемы безопасности;
проблемы с Railway production.

Раздели найденные проблемы на категории:

Критично до первого клиента.
Можно исправить после первого клиента.
Можно отложить до появления 5+ клиентов.
Можно отложить до появления 20+ клиентов.

В конце дай честную оценку готовности продукта по шкале от 1 до 10 и список оставшихся блокеров.