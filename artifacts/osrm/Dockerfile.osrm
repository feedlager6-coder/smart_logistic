# Self-hosted OSRM Backend for Railway
FROM ghcr.io/project-osrm/osrm-backend:latest

WORKDIR /data

# Download OSM data for Central Asia / CIS (or replace URL with your specific region from geofabrik.de)
ARG MAP_URL="http://download.geofabrik.de/asia/kazakhstan-latest.osm.pbf"

RUN apt-get update && apt-get install -y wget && rm -rf /var/lib/apt/lists/*
RUN wget -O map.osm.pbf "${MAP_URL}" && \
    osrm-extract -p /opt/car.lua map.osm.pbf && \
    osrm-partition map.osrm && \
    osrm-customize map.osrm && \
    rm map.osm.pbf

EXPOSE 5000

ENTRYPOINT ["osrm-routed", "--algorithm", "mld", "/data/map.osrm"]
