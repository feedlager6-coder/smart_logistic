# Руководство пользователя: Установка и настройка SmartRoute 1C Agent

SmartRoute 1C Agent привязывает компьютер с **1С:Предприятие 8.3 / 8.2** к SmartRoute и проверяет доступность COMConnector. Для полного обмена заказами и статусами дополнительно нужен интеграционный модуль 1С/BSL.

---

## ⚡️ Быстрый старт (5 простых шагов)

### Шаг 1. Скачайте агента
1. В веб-интерфейсе SmartRoute откройте **«Интеграции» → «Приложение-агент»**.
2. Для обычной установки нажмите **«Скачать установщик (.exe)»** и сохраните файл `SmartRoute_1C_Agent_Setup.exe`.
3. Если EXE блокируется политикой Windows или нужен portable-вариант, нажмите **«Скачать ZIP для ручной установки»**, сохраните `SmartRoute_1C_Agent.zip` и распакуйте его в удобную папку. В ZIP адрес текущего сервера уже записан в `config.json`.

### Шаг 2. Запустите установку
1. Дважды кликните по `SmartRoute_1C_Agent_Setup.exe`.
2. Нажмите **«Далее» → «Установить»**. Установщик создаст ярлык **SmartRoute 1C Agent** и запустит native-приложение.
3. При использовании ZIP запустите `Установка_SmartRoute.bat`. Он скопирует файлы в `%LOCALAPPDATA%\SmartRouteAgent`, создаст ярлык и откроет HTA-версию агента. Python устанавливать не нужно.

### Шаг 3. Получите код привязки в личном кабинете
1. В личном кабинете SmartRoute нажмите кнопку **«Сгенерировать код привязки»**.
2. Нажмите **«Скопировать»** (код формата: `SMARTROUTE-XXXX-YYYY`).

### Шаг 4. Привяжите агент к SmartRoute
1. В открывшемся окне приложения SmartRoute Agent на первой вкладке вставьте скопированный код.
2. Нажмите **«Привязать 1С к SmartRoute»** — индикатор статуса станет зелёным.

### Шаг 5. Выберите базу 1С и проверьте подключение
1. На вкладке **«Подключение 1С»** выберите вашу базу 1С из выпадающего списка.
2. Введите логин и пароль пользователя 1С.
3. Нажмите **«Проверить и сохранить подключение 1С»**.
4. На вкладке **«Синхронизация»** нажмите **«Проверить связь с сервером»**. Эта кнопка подтверждает доступность агента и сервера; выгрузка документов выполняется отдельным модулем 1С/BSL.

---

## 🔄 Как работает автоматическая синхронизация

После настройки native/HTA-приложение работает в фоновом режиме и отправляет heartbeat о доступности агента. Во вкладке **«Журнал событий»** видны результаты привязки и проверок. Полный обмен документами и статусами требует отдельного модуля 1С/BSL.

## Если привязка не проходит

1. В веб-кабинете создайте новый код: старые коды одноразовые и действуют 24 часа.
2. Проверьте, что в поле «Сервер SmartRoute» указан тот же адрес, который открыт в браузере.
3. Убедитесь, что на компьютере с 1С разрешены исходящие HTTPS-соединения к этому адресу.
4. Ошибка «Неверный код» означает, что код введён с опечаткой или был создан в другой организации.
5. Ошибка «Код уже использован» означает, что другой экземпляр агента уже занял этот код; создайте новый.
6. Для диагностики откройте вкладку «Журнал» агента. Пароль 1С и токен SmartRoute не передавайте в службу поддержки.

## Безопасность

Агент использует HTTPS с проверкой сертификата и имени сервера. Самоподписанные и просроченные сертификаты намеренно отклоняются. Не запускайте агент с обходом проверки TLS и не добавляйте ключ SmartRoute в `config.json` вручную.

## Ограничения и альтернативный способ

- Для работы Windows-агента требуется компьютер с Windows и установленной 1С:Предприятием. Сам SmartRoute API может работать на Linux.
- Полный двусторонний обмен с базой 1С через COMConnector возможен только на Windows с доступным COMConnector и отдельным модулем обмена. Наличие 1С на сервере SmartRoute не требуется.
- Если 1С работает на Linux-сервере или COMConnector недоступен, используйте вкладку **«Ручная настройка»** и интеграцию через BSL + SmartRoute API. Инструкция: [integration-1c.md](../../artifacts/api-server/docs/integration-1c.md).
- В репозитории есть готовые `SmartRoute_1C_Agent_Setup.exe` и `SmartRoute_Agent.exe`. Linux не может пересобрать или подписать Windows-бинарники; инструкция для сборки на Windows приведена в [DEVELOPER_GUIDE.md](DEVELOPER_GUIDE.md).

## Что отправить в поддержку при ошибке

Укажите адрес сервера, время ошибки и текст сообщения из журнала. Перед отправкой удалите из копии журнала любые пользовательские данные. Никогда не отправляйте пароль 1С, токен агента или содержимое `config.json`.
